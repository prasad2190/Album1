<%= form_for(@albumm) do |f| %>
  <% if @albumm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@albumm.errors.count, "error") %> prohibited this albumm from being saved:</h2>

      <ul>
      <% @albumm.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :descripton %><br />
    <%= f.text_area :descripton %>
  </div>
  
  
  
  
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
